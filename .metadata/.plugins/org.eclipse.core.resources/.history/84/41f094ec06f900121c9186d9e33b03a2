package com.adenon.sp.kernel.dialog;

public enum DialogType {

    STATEFUL(0),
    STATELESS(1);

    private int id;

    private DialogType(int id) {
        this.id = id;
    }

    public int getId() {
        return this.id;
    }

}
