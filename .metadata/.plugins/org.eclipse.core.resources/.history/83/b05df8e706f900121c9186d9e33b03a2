package com.adenon.sp.kernel.event.message;

public enum MessageType {

    BEGIN(0),
    CONTINUE(1),
    END(2),
    TERMINATE(3);

    private int typeId;

    private MessageType(int id) {
        this.typeId = id;
    }

    public int getId() {
        return this.typeId;
    }

}
