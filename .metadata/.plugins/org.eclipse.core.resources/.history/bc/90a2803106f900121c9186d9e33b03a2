package com.adenon.sp.kernel.utils;

public class StringUtils {

    private StringUtils() {
    }

    public static String cap(String str) {
        return str.substring(0, 1).toUpperCase() + str.substring(1);
    }

    public static boolean allUpperCase(String str) {
        String upperCased = str.toUpperCase();
        return str.equals(upperCased);
    }

    public static boolean allLowerCase(String str) {
        String lowerCased = str.toLowerCase();
        return str.equals(lowerCased);
    }

}
